""" tuples is immutable item in python it is a list that elements can't be change"""
tuples = (1,1,1,2,3)
count = tuples.count(1)
index = tuples.index(3)
print(count)
print(index)
# we can only visualize data from the tuples
print(tuples[0])